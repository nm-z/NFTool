
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.backend
    network_mode: host
    volumes:
      - ./backend:/app
      - ./workspace:/app/workspace
      - ./data:/app/data
      - /opt/rocm:/opt/rocm:ro
    environment:
      - API_KEY=plyo
      - PATH=/opt/rocm/bin:$PATH
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
      - PYTHONPATH=/app
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - "985"
      - "989"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8001/api/v1/health > /dev/null 2>&1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    network_mode: host
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      backend:
        condition: service_healthy
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8001
      - NEXT_PUBLIC_API_KEY=plyo
    healthcheck:
      test: ["CMD-SHELL", "node -e \"const http = require('http'); const req = http.request('http://localhost:3000', {method: 'HEAD'}, (res) => { if (res.statusCode === 200) process.exit(0); else process.exit(1); }); req.on('error', () => process.exit(1)); req.end();\""]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: always