{
  "name": "nftool",
  "version": "0.1.0",
  "description": "NFTool - Deep Learning Regression Tool",
  "scripts": {
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "frontend:build": "cd frontend && npm run build",
    "backend:build": "cd backend && pyinstaller --noconfirm --clean nftool-backend.spec",
    "backend:build:quick": "cd backend && pyinstaller --noconfirm nftool-backend.spec",
    "test:backend": "cd backend && .venv/bin/python -m pytest tests",
    "test:e2e": "export NEXT_PUBLIC_API_URL=http://127.0.0.1:8001 && export NEXT_PUBLIC_API_KEY=nftool-dev-key && (fuser -k 8001/tcp 3000/tcp || true) && (cd backend && export PYTHONPATH=$PYTHONPATH:. && .venv/bin/python src/api.py > /dev/null 2>&1 &) && sleep 3 && (cd frontend && npx playwright test)"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6"
  }
}
